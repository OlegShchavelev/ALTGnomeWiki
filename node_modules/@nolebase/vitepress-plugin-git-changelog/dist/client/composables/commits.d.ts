import { type Ref } from 'vue';
import type { PageData } from 'vitepress';
import type { Commit } from '../../types';
export declare function useCommits(pageData: Ref<PageData>): {
    commits: import("vue").ComputedRef<Commit[]>;
    update(data: Commit[]): void;
};
