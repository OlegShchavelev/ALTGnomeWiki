import type { ITrieNodeRoot } from '../ITrieNode.js';
import type { YieldResult } from './walkerTypes.js';
import { CompoundWordsMethod } from './walkerTypes.js';
/**
 * Ask for the next result.
 * goDeeper of true tells the walker to go deeper in the Trie if possible. Default is true.
 * This can be used to limit the walker's depth.
 */
export type HintedWalkerIterator = Generator<YieldResult, void, Hinting | undefined>;
export declare function hintedWalker(root: ITrieNodeRoot, ignoreCase: boolean, hint: string, compoundingMethod: CompoundWordsMethod | undefined, emitWordSeparator?: string): HintedWalkerIterator;
/**
 * Walks the Trie and yields a value at each node.
 * next(goDeeper: boolean):
 */
declare function hintedWalkerNext(root: ITrieNodeRoot, ignoreCase: boolean, hint: string, compoundingMethod: CompoundWordsMethod | undefined, emitWordSeparator?: string): HintedWalkerIterator;
export interface Hinting {
    goDeeper: boolean;
}
export declare const __testing__: {
    hintedWalkerNext: typeof hintedWalkerNext;
};
export {};
//# sourceMappingURL=hintedWalker.d.ts.map