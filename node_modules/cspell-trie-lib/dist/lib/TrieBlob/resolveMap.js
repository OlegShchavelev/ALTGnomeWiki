export function resolveMap(map, key, resolve) {
    const r = map.get(key);
    if (r !== undefined)
        return r;
    const v = resolve(key);
    map.set(key, v);
    return v;
}
//# sourceMappingURL=resolveMap.js.map