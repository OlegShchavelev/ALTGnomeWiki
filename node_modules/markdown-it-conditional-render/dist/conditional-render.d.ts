import { PluginWithOptions } from "markdown-it";
interface IMarkdownItOptions {
    ruleName: string;
    ifMarker: string;
    elseIfMarker: string;
    elseMarker: string;
    endIfMarker: string;
    validate(condition: string): boolean;
    evaluate(condition: string, value: any): boolean;
}
export declare const plugin: PluginWithOptions<IMarkdownItOptions>;
export {};
